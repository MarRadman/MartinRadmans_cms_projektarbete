import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Din primärfärg
    },
    secondary: {
      main: "#dc004e", // Din sekundärfärg
    },
  },
});

export default theme;
